#include <stdio.h>
#include <math.h>
int main() {
    // Write C code here
   int a;
   scanf("%d",&a);
   if(a>0)
   {
       printf("positive");
   }
   else if(a<0)
   {
       printf("negative");
   }
   else
   {
       printf("zero");
   }
    return 0;
}
