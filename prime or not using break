#include <stdio.h>

int main() {
    // Write C code here
    int n,i,c=0;
    scanf("%d",&n);
    for(i=2;i<n;i++)
    {
        if(n%i==0)
        {
            c=1;
            break;
        }
    }
    if(c==0&&n!=1)
    printf("prime number");
    else
    printf("not a prime number");

    return 0;
}
